{% if filetype_loop_jinja2 == "csv" %}VLAN,Interface,Name,State,Shutdown,MTU{% for vlan in to_parse_vlan %}{% for interface in to_parse_vlan[vlan].interfaces %}
{{ vlan }},{{ interface }},{{ to_parse_vlan[vlan].name }},{{ to_parse_vlan[vlan].state }},{{ to_parse_vlan[vlan].shutdown }},{{ to_parse_vlan[vlan].mtu }}{% endfor %}{% endfor %}{% else %}{% if filetype_loop_jinja2 == "md" %}
# Show VLAN
| VLAN | Interface | Name | State | Shutdown | MTU |
| ---- | --------- | ---- | ----- | -------- | --- |{% for vlan in to_parse_vlan %}{% for interface in to_parse_vlan[vlan].interfaces %}
| {{ vlan }} | {{ interface }} | {{ to_parse_vlan[vlan].name }} | {{ to_parse_vlan[vlan].state }} | {{ to_parse_vlan[vlan].shutdown }} | {{ to_parse_vlan[vlan].mtu }} |{% endfor %}{% endfor %}{% else %}
<html><body>
<h1>Show VLAN</h1>
<table style="width:100%">
  <tr>
    <th>VLAN</th>
    <th>Interface</th>
    <th>Name</th>
    <th>State</th>
    <th>Shutdown</th>
    <th>MTU</th>
  </tr>{% for vlan in to_parse_vlan %}{% for interface in to_parse_vlan[vlan].interfaces %}
  <tr>
    <td>{{ vlan }}</td>
    <td>{{ interface }}</td>
    <td>{{ to_parse_vlan[vlan].name }}</td>
    <td>{{ to_parse_vlan[vlan].state }}</td>
    <td>{{ to_parse_vlan[vlan].shutdown }}</td>
    <td>{{ to_parse_vlan[vlan].mtu }}</td>
  </tr>{% endfor %}{% endfor %}
</table>
</body></html>{% endif %}{% endif %}